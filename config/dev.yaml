# Development configuration
dashboard:
  host: 127.0.0.1
  port: 8888
  debug: false  # Set to true for auto-reload (requires full disk access on macOS)
  password: null  # Set a password to protect dashboard (e.g., "my-secret-password"), null = no auth

# Inherits Nest config structure
nest:
  storage_path: ~/.kastrel-dashboard/data/
  encryption_key: dev-key-not-for-production

# Dashboard-specific
database:
  url: sqlite+aiosqlite:///~/.kastrel-dashboard/dashboard.db

demo:
  local_data_path: ./demo_data/
  enable_mock_perches: true

# Perch AI Service
perch_service:
  url: http://localhost:9000
  timeout: 60  # seconds for model generation
  connection_timeout: 10  # seconds for connection

logging:
  level: DEBUG
